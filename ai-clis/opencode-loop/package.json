{
  "name": "opencode-loop",
  "version": "0.1.0",
  "description": "Iterative implement + review loop for OpenCode: runs `opencode run --command implement <ticket>`, then `opencode run --command review <ticket>`, and repeats until the review's Final Decision is exactly LGTM.",
  "type": "module",
  "bin": {
    "opencode-loop": "./src/cli.ts"
  },
  "files": ["src/"],
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "keywords": ["opencode", "cli", "review", "loop", "automation", "ai"],
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "1.3.6",
    "typescript": "^5.7.3"
  }
}
